{
  "name": "frontend-native",
  "version": "0.0.1",
  "scripts": {
    "dev": "electrobun dev",
    "build": "electrobun build",
    "start": "bun run build && bun run dev"
  },
  "dependencies": {
    "frontend-core": "workspace:*",
    "electrobun": "latest"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
