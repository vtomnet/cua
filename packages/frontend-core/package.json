{
  "name": "frontend-core",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir=dist --format=esm --target=node && bun run build:types",
    "build:types": "tsc --project tsconfig.build.json"
  },
  "dependencies": {
    "core": "workspace:*",
    "llama-tokenizer-js": "^1.2.2",
    "onnxruntime-web": "^1.23.0",
    "whisper-feature-extractor": "workspace:*"
  },
  "devDependencies": {
    "svelte": "^5.39.10",
    "typescript": "^5.9.3"
  }
}
